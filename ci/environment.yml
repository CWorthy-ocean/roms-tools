name: romstools
channels:
  - conda-forge
  - nodefaults
dependencies:
  - python>=3.9
  - numpy
  - xarray
  - matplotlib
  - cartopy
  - pooch
  - packaging
  - netcdf4
  - numba
  - pytest
  - flake8
  - black
  - scipy
  - gcm_filters
  - ipykernel # not strictly necessary but this is nice to run notebooks in this env to test
